{
  "name": "lionix-task-report",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "packageManager": "npm@11.17.0",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "start": "node index.js",
    "dev": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
